/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: [
      'images.unsplash.com',
      'profile-images.xing.com',
  ],
  },
}

module.exports = nextConfig
